# 切换应用视图

应用集详情页面提供了 2 种视图对应用展示："图标视图"和"列表视图"。

右上角操作菜单中的切换视图按钮为"临时"按钮，点击可以"频繁且随意地"在两种视图间切换。如果要将固定设置为某个视图，需要在设置 → 通用 → 应用视图中设置，这样每次进入应用集详情页应用的排列就会变成设置好的视图。

![切换应用视图](/app-management/switch-view.png)
