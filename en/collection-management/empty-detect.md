# Detect Empty Collection

**Settings -> Empty Collection Detection -> Auto-Detect Empty Collections**

When this feature is enabled, deleting the last remaining application within a Collection Details page will trigger an automatic prompt asking if you would like to delete the empty collection as well.